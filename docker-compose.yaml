services:
  backend:
    image: node:22-alpine
    working_dir: /app
    command: sh -c "npm ci && npm run backend"
    environment:
      PORT: 3000
    ports:
      - '${BACKEND_PORT:-3000}:3000'
    volumes:
      - ./:/app
    restart: unless-stopped
